<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Showing - <%= @post.name %></title>
  </head>
  <body>
    <h1><%= @post.name %></h1>
    <p><%= @post.content %></p>
    <form method="POST" action=<%="/posts/#{@post.id}/delete"%> >
      <input id="hidden" type="hidden" name="_method" value="delete"/>
      <input type="submit" id="submit" value="delete"/>
    </form>
  </body>
</html>
